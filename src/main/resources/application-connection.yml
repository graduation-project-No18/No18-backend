spring:
  datasource:
    driver-class-name: org.h2.Driver
    url:
    username: root
    password: root

  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect:
        default_batch_fetch_size: 1000